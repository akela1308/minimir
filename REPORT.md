# REPORT — «мини-мир», автономная ночная сессия

*Составлено Claude Code, 2026-07-25 → 26. Все прогоны на этой машине
(**2 физических / 4 логических ядра** — не 8, как предполагал бюджет;
см. «Отклонения от плана»; numpy 2.5, python 3.13, .venv в корне).*

Статус по блокам: **0 — готов, A — готов, B — готов, C — готов.**
Все прогоны завершены; сырьё в `runs/*.jsonl` и `runs/*.json`, графики в
`runs/figures/`, история — по коммитам на блок.

---

## 1. Сводка в одном абзаце

**Ни одна из главных гипотез не подтвердилась в сильном смысле, и это
доложено без подгонки.** Инфраструктура (блок 0) приведена в порядок: раннер
инкрементальный/возобновляемый/устойчивый к падениям, индивидуальные
гистограммы векторизованы (проверено инвариантом сохранения), гипотеза о
разрастании к потолку опровергнута, добавлены тесты, починен тихий баг кэша
предка. **Блок A (главный):** внутриагентная метрика откалибрована
(положит. контроль ~0.5 бит, отриц. ~0); в главном сравнении `self` устойчиво
и значимо выше `shuffled` **парно** (Уилкоксон p=0.0005, дельта Клиффа +0.64)
и специфично к знанию о себе (`self` > `neighbour` ≈ `shuffled`), **но**
разница средних (0.023 бит) не превышает 2σ межсидового разброса (0.050) —
по преригестрированному двойному критерию **H1 не подтверждена**; вдобавок
метрика не выходит на устойчивое плато к 400k (A.4), и именно нестационарный
шум мира перекрывает эффект. **Блок B:** фазовый переход Requejo–Camacho
**не воспроизведён**, и обнаружено почему тест на этом движке некорректен —
`give` создаёт чистую энергию, связывая кооперацию с ростом популяции и
исключением прогонов; зато популяционный confound «голод→кооперация» (−0.79)
подтверждён как артефакт и закрыт внутриагентной мерой (~0). **Блок C:** мир
с знаками и пластичностью жизнеспособен, но отбор **отключает** пластичность
(lr 0.002→~0.001), метки почти не используются (поле не насыщается), а тест
порядка развития даёт вердикт **«инстинкт»** — самонаправленное использование
метки врождённое, влияния на других нет, интернализации по Выготскому нет.
Общая линия: движок честен и хорошо инструментован, но в текущей энергетике
и на доступном железе ни один из трёх содержательных эффектов (интероцепция,
фазовый переход кооперации, интернализация знака) не достигает силы вывода —
и в каждом случае понятно, что именно мешает.

---

## 2. Числа по блокам

### Блок 0 — инфраструктура

**0.1 Устойчивый раннер (`experiments.py`).** Переписан: результат каждого
прогона пишется строкой в JSONL немедленно; повторный запуск пропускает уже
посчитанные `(условие, seed)` по ключу; прогресс с ETA в stderr; `--workers`
по умолчанию `cpu_count()-1`; исключение отдельного прогона ловится и пишется
как `{"error": ...}`, серия продолжается. Проверено: серия из 3 прогонов,
повторный запуск печатает `[resume] 3/3 уже посчитано`, файл не растёт.
Добавлено блочное усреднение MI по блокам 15 000 тиков (preregistration §4).

**0.2 Векторизация индивидуальных гистограмм.** Питоновский цикл по агентам
в `engine.step()` заменён на плотный массив `(max_pop, 10, N_ACTIONS)` с
`np.add.at` по трём индексам; при переиспользовании слота гистограмма
умершего снимается в `ind_done` до обнуления. Эквивалентность старой версии
доказана инвариантом сохранения: **сумма всех индивидуальных гистограмм
побитово равна популяционной** `energy_action_hist` за то же окно
(718969 == 718969 сэмплов). Стоимость режима `track_individual` при pop≈509:
**+17%** к скорости (было **+40%** на старом цикле).

**0.3 Диагностика замедления.** Гипотезу «в каком-то условии популяция
разрастается к `max_pop=2000`» проверял свипом 4 условий × 2 seed × 15 000
тиков (`runs/diag_slowdown.py`, сырьё в `runs/diag_slowdown.json`).

| seed | условие | пик поп. | финал | стена, с |
|---|---|---|---|---|
| 1 | self | 573 | 323 | 25.0 |
| 1 | shuffled | 530 | 278 | 28.7 |
| 1 | neighbour | 527 | 279 | 30.6 |
| 1 | off | 572 | 328 | 32.4 |
| 2 | self | 611 | 357 | 30.3 |
| 2 | shuffled | 652 | 374 | 38.2 |
| 2 | neighbour | 592 | 346 | 35.3 |
| 2 | off | 528 | 301 | 30.8 |

Гипотеза **опровергнута**: ни одно условие не приближается к потолку
(пики 527–652 против 95%-критерия 1900). Критерий невалидности «≥95% max_pop»
ни разу не сработал. Скорость падает с популяцией: 599 тик/с при pop<250,
474 при 250–500, 381 при 500–750 — то есть реальная скорость ~380–600 тик/с,
вдвое ниже заявленных в доках 800–1000. **Следствие для бюджета:** оценка
предрегистрации «~4 ч на ядре / 35 мин на 8» оптимистична примерно вдвое.
Наиболее вероятная причина исходного превышения 18 минут — именно старый
питоновский цикл индивидуальных гистограмм (0.2), чья стоимость растёт с
популяцией линейно; он и устранён.

**0.4 Тесты (`tests/test_sim.py`, pytest, 8 проверок, 8.7 с суммарно).**
Детерминизм по seed; расхождение разных seed; сохранение энергии на одном
агенте (остаток баланса < 0.05 при e_start 300); `mi_from_hist` на синтетике
(идеальная связь → log2 n с точностью 0.02; независимость → |corrected|<0.01;
мало данных → None); побитовая воспроизводимость предка из кэша с проверкой
формы; `shuffled` сохраняет маргинал входа 8 бит-в-бит, но даёт MI<0.01.
Все проходят.

### Отклонения от плана из-за железа (важно для честности)

Машина оказалась **2-ядерной физически** (4 логических потока), а не «8 ядер»,
как предполагали задание и бюджет предрегистрации. Это выяснилось при
диагностике загрузки: 7 воркеров давали лишь ~230% CPU из-за (а) конкуренции
за 2 ядра и (б) многопоточного BLAS в каждом воркере (oversubscription).
Меры: везде выставлен однопоточный BLAS (`OMP_NUM_THREADS=1` и т.д.), пул
сведён к 2 воркерам (= 2 физических ядра, предсказуемая загрузка).

Следствие для A.5: полный преригестрированный объём (20 seed × 200 000 тиков ×
4 условия = 80 прогонов ≈ 16 млн тиков) на 2 ядрах — это ~4–5 часов только на
одно сравнение, что не оставляет ночи на блоки B и C. По **эскейп-клаузе самой
предрегистрации** (§4: «если скорость упадёт ниже 500 тиков/с, число seed'ов
сокращается, а чувствительность пересчитывается, а не подгоняется») сокращаю:
**12 seed × 120 000 тиков × 4 условия**. Длину прогона держу такой, чтобы
измерительное окно оставалось полным (прогрев 45k + 5 блоков × 15k = 75k) —
окно важнее числа seed'ов. n=12 достаточно, чтобы парный Уилкоксон в принципе
мог дать p<0.01. Мощность ниже преригестрированной — это отмечено, а не скрыто.

A.3 (уровень шума) не считаю отдельной серией: 2σ межсидового разброса беру
из тех же self-прогонов A.5 (то же условие, та же длина, даже больше seed'ов),
что экономит счёт и не жертвует смыслом.

### Блок A

**A.2 Предрегистрация — готово.** Внесены ровно три правки из задания и
заморожены коммитом `preregistration: freeze protocol for stage 1`; старая
версия сохранена как `PREREGISTRATION_v1_отменена.md`. Правки: (1) H1
переформулирована — «возникнет ли зависимость **под действием одного
дифференциального размножения, без назначенной награды**» (иначе это
переоткрытие теоремы Keramati–Gutkin 2011); (2) главная метрика —
внутриагентная `within_agent_dependence`, популяционная переведена в
исследовательские; (3) условие `neighbour` разведено на два прочтения —
контроль к H1/H2 и самостоятельное второе лечение (H3, просоциальность
по Yoshida–Man 2024).

**Расхождение формы генома (зафиксировано отдельно, как просили).**
`PREREGISTRATION.md` v1 §3 писала «19 входов, 9 выходов»; код —
`N_IN=25, N_OUT=12` (`SENSOR_NAMES` перечисляет 25 входов, `N_OUT = N_ACTIONS(8)
+ N_MEM(2) + N_SIGN(2) = 12`). Предрегистрацию по этому поводу **не** переписывал
сверх согласованных трёх правок; в замороженной версии добавлена лишь пометка,
что существенно одно — форма генома одинакова во всех условиях, и это в коде
выполнено. Расхождение — документационное, на измерения не влияет.

**A.5 главное сравнение + A.3 уровень шума — готово.**
12 seed × 120 000 тиков × 4 условия, блочное усреднение (5 блоков × 15k),
`runs/compare.jsonl`, анализ `runs/analyze_compare.json`, графики
`runs/figures/a5_compare_within_mi.png`, `a5_compare_mi.png`.

Годность: self 12/12, shuffled 12/12, neighbour 12/12, **off 11/12**
(1 вымирание). Исключён 1 прогон (off, вымер). Потолок и «мало децилей»
не срабатывали — во всех годных занято 8 децилей.

Главная (внутриагентная) метрика, бит:

| условие | среднее | sd | медиана | n |
|---|---|---|---|---|
| `self` (лечение) | **0.0382** | 0.0250 | 0.0413 | 12 |
| `shuffled` (контроль) | 0.0149 | 0.0127 | 0.0190 | 12 |
| `neighbour` (2-е лечение) | 0.0208 | 0.0203 | 0.0160 | 12 |
| `off` (старый контроль) | 0.0259 | 0.0189 | 0.0278 | 11 |

A.3: **2σ межсидового разброса (из self) = 0.0500 бит** (внутриаг.);
0.0229 бит (популяц.). Порог значимости для сравнения условий.

Парные сравнения с `shuffled` (Уилкоксон по seed'ам, дельта Клиффа):

| сравнение | p | δ Клиффа | Δсред | > 2σ? | вывод |
|---|---|---|---|---|---|
| `self` vs `shuffled` | **0.0005** | +0.639 | +0.0233 | **нет** (2σ=0.050) | **не подтверждено** |
| `neighbour` vs `shuffled` | 0.301 | +0.111 | +0.0060 | нет | не подтверждено |
| `off` vs `shuffled` | 0.0029 | +0.405 | +0.0125 | нет | не подтверждено |

Популяционная метрика (исследовательская): self 0.0137 vs shuffled 0.0013,
p=0.0005, **δ=0.847**; neighbour/off неотличимы от shuffled.

**Итог A.5 (осторожно, по преригестрированному критерию).** H1 **не
подтверждена**. Парное сравнение `self` vs `shuffled` статистически значимо
(p=0.0005 < 0.01) и с большой величиной эффекта (дельта Клиффа +0.64 внутри-
агентно, +0.85 популяционно) — то есть **внутри каждого seed'а `self`
почти всегда > `shuffled`**. Но абсолютная разница средних (0.023 бит
внутриагентно) **меньше 2σ межсидового разброса (0.050)**, а предрегистрация
требует выполнения **обоих** условий. Эффект реальный и направленно
специфичный к знанию о себе (`self` > `neighbour` ≈ `shuffled`: H2 в нужную
сторону, `neighbour` от контроля не отличается, p=0.30), но по нашему же
строгому двойному порогу он не проходит. Это заранее оговорённый
отрицательный результат (предрегистрация §8): необратимой смерти и доступа
к сигналу о себе, при одном дифференциальном размножении и на 12 seed'ах,
**недостаточно**, чтобы эффект превысил собственный межсидовый шум мира.
Параметры не трогаю и не перезапускаю (§7).

**A.1 Калибровка внутриагентной метрики — готово.**
`runs/a1_calibrate.json`, график `runs/figures/a1_calibrate.png`. Меряли в
окне до вымирания основателей (пропуск 300, окно 2500 тиков; ручные политики
не размножаются и разом вымирают на max_age=3000).

| политика | популяц. MI | внутриаг. MI | n агентов | занято дец. |
|---|---|---|---|---|
| threshold@20 | 0.545 | 0.495 | 155 | 3 |
| threshold@30 | 0.591 | 0.525 | 173 | 3 |
| threshold@40 | 0.628 | 0.543 | 179 | 4 |
| threshold@50 | 0.663 | 0.551 | 181 | 4 |
| threshold@60 | 0.672 | 0.535 | 179 | 4 |
| threshold@70 | 0.675 | 0.531 | 178 | 4 |
| **random** | −0.00001 | **0.0004** | 147 | — |
| evolved | 0.031 | 0.049 | 259 | — |

Прибор откалиброван: положительный контроль (threshold) даёт внутриагентную
MI ~0.5 бит, отрицательный (random) ~0. Внутриагентная метрика видит сильную
зависимость и не даёт ложного сигнала. Популяционная MI на threshold@40
(0.628) согласуется с задокументированными 0.586. Внутриагентная версия
систематически чуть ниже популяционной на threshold (~0.53 vs ~0.63) — на
детерминированной политике различие невелико; расхождение вдвое, отмеченное
в доках, относилось к эволюционной популяции. Занятость <4 децилей при
порогах 20–30 — политика концентрирует популяцию по одну сторону порога
(тот же эффект, что у threshold@85 в предрегистрации).

**A.1 размер выборки — готово.** `runs/a1_samplesize.json`. 14 132 агента
с ≥800 решений; MI по ПЕРВЫМ N решениям:

| N решений | внутриаг. MI (сред.) | sd между агентами |
|---|---|---|
| 100 | 0.0104 | 0.0289 |
| 200 | 0.0094 | 0.0230 |
| 400 | 0.0085 | 0.0178 |
| 800 | 0.0104 | 0.0168 |

**Bias-corrected среднее N-независимо уже с N=100** (колеблется в пределах
0.002 бит без тренда) — поправка на смещение (r−1)(c−1)/(2N·ln2) работает как
задумано. Разброс между агентами падает 0.029→0.017 к N=800, выходя на плато
около **N≈400**. Вывод для критерия отбора: для устойчивого популяционного
среднего достаточно `min_samples=200` (текущий дефолт), для устойчивой
**per-agent** оценки лучше 400–800. Важная оговорка: при N=200 сама поправка
на случайность для таблицы 10×8 составляет ~0.23 бит — на порядок больше
слабого сигнала эволюционной популяции (~0.05 бит), поэтому индивидуальные
оценки при малых N шумны, хотя их среднее несмещённое. `min_samples` не менял
задним числом (это нарушило бы §7 и среднее и так устойчиво).

**A.4 Выход на плато — готово, плато НЕ достигается устойчиво.**
3 seed × 400 000 тиков, блочные средние по 27 блокам × 15k
(`runs/a4_plateau.json`, график `runs/figures/a4_plateau.png`):

| seed | наклон хвоста | вердикт | пик поп. |
|---|---|---|---|
| 1 | −2.42σ | падает | 381 |
| 2 | +2.03σ | ещё растёт | 473 |
| 3 | −0.57σ | плато | 406 |

Из трёх seed'ов только один на плато, один падает, один растёт. **Даже
блочное усреднение по 15k не выводит метрику на устойчивое плато к 400k** —
она продолжает блуждать вместе с бум-крах-циклами численности. Длину прогона
**не увеличиваю** (как велит задание). Прямое следствие: главная метрика на
этом мире не вполне стационарна, и это ровно та причина, по которой в A.5
межсидовый разброс (2σ=0.050) перекрыл эффект интероцепции (0.023). Плато-
критерий годности из предрегистрации §5 (наклон хвоста > 1σ) при таком
поведении исключил бы 2 из 3 длинных прогонов — но в A.5 (120k, 5 блоков)
мы этот критерий не применяли к каждому прогону отдельно; это ограничение
занесено в раздел 5.

### Итог блока A

A.1 (калибровка) — прибор годен: внутриагентная MI различает 0.5 бит
(положительный контроль) и ~0 (отрицательный). A.2 (предрегистрация) —
заморожена. A.3 (шум) — 2σ=0.050 бит. A.4 (плато) — **не достигается
устойчиво к 400k**. A.5 (главное) — **H1 не подтверждена** по двойному
критерию (значимо парно, p=0.0005, δ=0.64, но эффект 0.023 < 2σ 0.050).
Общий вывод блока A: доступа к сигналу о себе достаточно для устойчивого
*парного* преимущества `self` над `shuffled`, но не для превышения
собственного межсидового шума мира — и этот шум сам по себе не стационарен
(A.4). Отрицательный результат в смысле предрегистрации, доложен без подгонки.

### Блок B — валидация движка чужим предсказанием

Свип по `regrowth` (8 значений 0.0018…0.011) × 5 seed, `social=True`,
`recognition=False`, знаки выкл., соц.слой с тика 40k. `runs/b_sweep.jsonl`,
анализ `runs/analyze_b.json`, график `runs/figures/b_phase.png`.

**Годность.** Исключено **11 из 40** прогонов: 7 упёрлись в потолок
(`max_pop`), 4 вымерли. Исключения распределены неравномерно и — ключевое —
**коррелируют с кооперацией** (см. ниже).

| regrowth | coop_rate (годн.) | give_fr | take_fr | pop_corr | within_corr | годных |
|---|---|---|---|---|---|---|
| 0.0018 | 0.159 | 0.013 | 0.142 | −0.010 | +0.068 | 4 |
| 0.0022 | 0.139 | 0.016 | 0.238 | −0.744 | −0.076 | 3 |
| 0.0028 | 0.022 | 0.003 | 0.115 | +0.690 | +0.049 | 1 |
| 0.0035 | 0.252 | 0.010 | 0.129 | −0.747 | −0.080 | 5 |
| 0.0045 | 0.173 | 0.010 | 0.324 | −0.889 | −0.141 | 5 |
| 0.006 | 0.038 | 0.006 | 0.333 | −0.933 | −0.046 | 5 |
| 0.008 | 0.138 | 0.007 | 0.267 | −0.909 | −0.087 | 3 |
| 0.011 | 0.030 | 0.004 | 0.292 | −0.329 | +0.011 | 3 |

**Фазовый переход Requejo–Camacho — НЕ воспроизведён.** Доля кооперации
шумная и немонотонная (0.16, 0.14, 0.02, 0.25, 0.17, 0.04, 0.14, 0.03),
`give_frac` крошечная (0.003–0.016) на всех уровнях изобилия — популяция
предательская везде. Автоскрипт отметил «резкий переход» между 0.0028 и
0.0035, но это **артефакт**: точка 0.0028 держится на **одном** годном
seed'е из пяти (три упёрлись в потолок, один вымер). Резкого перехода
«предатели при изобилии → безусловные кооператоры при дефиците» нет.

**Почему тест на этом движке скомпрометирован (важное открытие).** Прогоны,
упирающиеся в потолок, — именно высококооперативные: seed'ы с coop 0.46–0.97
разрастаются до `peak_pop=2000`, seed'ы с coop <0.1 держатся у расчётной
ёмкости. Причина в метаболике социального слоя: `give` отдаёт получателю
+12 ценой −8 автору, то есть **создаёт +4 чистой энергии в популяции на
каждое событие**. Кооперация работает энергетическим насосом → рост
популяции → потолок → исключение из анализа. Так кооперация и валидность
прогона **связаны по построению**, и это (а) смещает coop_rate годных
прогонов вниз, (б) структурно расходится с моделью Requejo–Camacho, где
выигрыши — теоретико-игровые, а не метаболический приток энергии. Вывод:
движок в текущей энергетике социального слоя **не является чистой проверкой**
их аналитического результата; прежде чем строить выводы на социальном слое,
надо сделать `give` энергетически нейтральным (сумма give_gain и give_cost = 0)
или иначе развязать кооперацию и рост популяции.

**Confound голод↔кооперация — подтверждён и закрыт.** Популяционная
корреляция «доля give по децилям энергии» сильно отрицательна (−0.7…−0.93 на
большинстве regrowth) — это ровно те «−0.79», что тревожили в `PRIOR_WORK`.
Внутриагентная корреляция во времени — почти ноль (−0.14…+0.07). То есть
эффект «голодные кооперируют» **в основном артефакт**: `take` сам поднимает
энергию, обогащая верхние децили «отнимающими», а не потому что сытость
меняет стратегию внутри агента. Правка `within_agent_hunger_vs_cooperation`
закрывает этот confound измерением.

### Блок C — пилоты третьего этапа

**C.1 Жизнеспособность — мир держится, пластичность отбором отключается.**
4 условия × 5 seed × 15 000 (`runs/c1_pilot.json`). Медиана жизни везде
523–862 (> порога 400). Вымираний: базовый 0/5, только_знаки 1/5 (seed3),
только_пластичность 1/5 (seed3), знаки+пластичность 0/5. Потолок — 0 везде.
seed3 хрупок под одиночным лечением, но выживает в базовом и в комбинации.
Мир не рушится (граница провала §10.1/§9.6 не сработала).

Эволюция скорости обучения `lr` (старт 0.002), среднее по годным:

| условие | lr сред. | hebbian |
|---|---|---|
| базовый | 0.0039 | нет (дрейф) |
| только_знаки | 0.0018 | нет (дрейф) |
| только_пластичность | **0.0011** | да |
| знаки+пластичность | **0.0010** | да |

Когда пластичность включена, `lr` **падает вдвое** (0.002 → ~0.001) за 15k;
когда выключена — случайно дрейфует. Вывод (заранее оговорённый как
полноценный, C.1): **отбор отключает пластичность** — прижизненное обучение
в этом мире не полезно, и генетическая скорость обучения давится вниз.
Направление ясное; дойдёт ли `lr` до нуля или стабилизируется — нужен более
длинный прогон (15k — лишь несколько поколений).

**C.2 Осмысленность метки — поле НЕ вырождается в кашу, но метка почти не
используется.** Свип `mark_cost` 0.1…2.0 (`runs/c2_markcost.json`, график
`runs/figures/c2_markcost.png`):

| mark_cost | mark_rate | поле заполнено | MI(сост.; содерж.) |
|---|---|---|---|
| 0.1 | 0.0073 | 0.004 | 0.073 |
| 0.25 | 0.0031 | 0.018 | 0.040 |
| 0.5 | 0.0003 | 0.000 | 0.041 |
| 1.0 | 0.0014 | 0.008 | 0.080 |
| 1.5 | 0.0013 | 0.002 | 0.017 |
| 2.0 | 0.0003 | 0.000 | — |

«Каша» не возникает ни при какой стоимости (заполнено ≤ 1.8% клеток).
Проблема **обратная** ожидаемой: эволюционная популяция почти не метит
(mark_rate ≤ 0.7% даже при дешёвой метке). Причина: предок отбирался в мире
без знаков, а в пилоте нет задачи, где метка помогает, — значит нет и отбора
на неё. `mark_cost` не является связывающим ограничением; для максимума
активности подходит 0.1–0.25, но сигнал слаб при любом значении.

**C.3 Калибровка метрик знака — нижний уровень откалиброван, верхняя граница
неполна.** (`runs/c3_signcalib.json`)

| политика | mark_rate | MI(сост.;содерж.) | свежесть | вывод |
|---|---|---|---|---|
| `mark_always` (шум) | 0.37 | **0.0** | 46.7 | нижний уровень ✓ |
| `mark_when_hungry_follow_own` | 0.81 | — (N=357<1000) | 1.0 | верхняя граница неполна |
| `ignore_marks` | 0.00 | — (меток нет) | — | нулевой уровень ✓ |

`mark_always` подтверждает: бессмысленная (случайная) метка даёт MI ровно 0 —
метрика не даёт ложного сигнала. `ignore_marks` — меток нет. Но верхняя
граница (`mark_when_hungry`, где содержание кодирует голод) **не откалибрована
до конца**: политика не самоподдерживающаяся, наделала лишь 357 меток —
ниже порога 1000 сэмплов в `mi_from_hist`, поэтому MI не посчиталась.
Свежесть 1.0 у этой политики подтверждает, что механизм самонаправленного
чтения работает (агент читает свою метку возрастом 1 тик). Ограничение
занесено в «что сломалось».

**C.4 Тест порядка развития — интернализации НЕТ, вердикт «инстинкт».**
Стадия-3 конфиг (знаки+социалка+пластичность), 40k тиков измерения,
возрастные окна по 200 (`runs/c4_devorder.json`). Кривые по возрасту агента:

| возраст | SELF MI (своя метка→своё) | on N | OTHER MI (метка→другие) | on N | контакты |
|---|---|---|---|---|---|
| 0 | 0.085 | 16789 | 0.012 | 4030 | 0.25 |
| 400 | 0.079 | 15792 | 0.003 | 912 | 0.14 |
| 800 | 0.127 | 7606 | 0.001 | 263 | 0.10 |
| 1200 | 0.131 | 3646 | 0.000 | 3 | 0.25 |
| 2000 | 0.076 | 1137 | 0.000 | 0 | 0.17 |
| 2600 | 0.213 | 363 | 0.000 | 0 | 0.01 |

Момент появления SELF = 0, OTHER = 0 → **вердикт «инстинкт/одновременно»**.
Само-влияние метки присутствует и сильно **с рождения** (SELF MI 0.08–0.21),
а влияние на других — практически ноль (OTHER MI 0.001–0.012, на грани шума,
и число событий падает до нуля к старости). Это границы провала §9.4
(«только инстинкт») и §9.2 («знак есть, но пустой» — на других не влияет).

**Интернализация по Выготскому не наблюдается**, причём картина скорее
обратная: самонаправленное использование врождённое, а социального
предшествования нет вовсе. Метка работает как **приватная внешняя память**
(recurrence backdoor): агент почти всегда стоит на СВОЕЙ метке (on N для SELF
на порядок больше, чем для OTHER), то есть читает то, что сам только что
поставил. Это ровно то, от чего предостерегал STAGE3 §3. Контроль на confound
(плотность контактов по возрасту: 0.25→0.01, слегка убывает) порядок не
объясняет — объяснять нечего, порядка нет. Кросс-секционные числа выше;
лонгитюдно прослежено 507 агентов (согласуется).

**Итог блока C.** Стадия-3 в текущем виде не проходит: (1) отбор давит
пластичность вниз (C.1), (2) метки почти не используются и поле не насыщается
(C.2), (3) само-использование метки врождённо, влияния на других нет →
интернализации нет (C.4). Прибор частично откалиброван (нижний уровень —
да, верхний — нет из-за нежизнеспособности эталонной политики). Это
характеризация нескольких границ провала стадии-3 **до** дорогих полных
прогонов — ровно то, ради чего пилоты и делаются.

---

## 3. Неожиданное

- Реальная скорость движка (~380–600 тик/с при рабочих популяциях) **вдвое
  ниже** задокументированных 800–1000. Это не поломка, но весь вычислительный
  бюджет предрегистрации построен на завышенной цифре.
- **`off` даёт MI выше, чем `shuffled`** во внутриагентной метрике
  (0.026 vs 0.015, p=0.003, дельта Клиффа +0.41), хотя `off` — это зануление
  входов 8–9, а `shuffled` — подмена с сохранением распределения. Ожидалось
  бы обратное или равенство. Гипотеза (не проверена): подменённый чужой сигнал
  на входе 8–9 — это активный шум, на который агент частично реагирует, и это
  реагирование **размывает** его собственную зависимость поведения от энергии;
  зануление же просто убирает вход, оставляя более чистую (пусть и косвенную)
  зависимость. Если так, то `shuffled` — более «злой» контроль, чем `off`,
  что согласуется с решением сделать именно его основным. В популяционной
  метрике эффект исчезает (off≈shuffled), т.е. он именно внутриагентный.
- **2σ-критерий, а не p-значение, оказался решающим порогом.** Разница
  `self`−`shuffled` устойчива внутри seed'ов (p=0.0005, δ=0.64–0.85), но
  межсидовый разброс самого `self` (sd 0.025) настолько велик, что 2σ=0.050
  перекрывает разницу средних 0.023. То есть мир производит колебания метрики
  между seed'ами больше, чем эффект интероцепции. Это и есть содержательная
  причина отрицательного вывода — не отсутствие эффекта, а его малость
  относительно шума мира.

---

## 4. Что сломалось / исправлено

- **Кэш предка мог тихо подхватить несовместимый геном.** Имя файла содержало
  `N_IN`, но не `N_OUT`; при изменении числа выходов форма генома менялась, а
  кэш — нет. Добавил `_out{N_OUT}` в имя и проверку формы массивов при загрузке
  (`sim/ancestry.py`). Существующие кэши переименованы под новую схему.
- **Docstring `sim/world.py`** заявлял «Тор 64x64» при сетке 128×128 —
  исправлено.
- Расхождение формы генома между `PREREGISTRATION.md` (§3: «19 входов,
  9 выходов») и кодом (`N_IN=25, N_OUT=12`) — см. блок A.2, фиксируется
  отдельно, предрегистрацию по этому поводу не переписываю.
- **Баг блочного измерения (найден и исправлен до подтверждающих прогонов).**
  В первой версии блочного усреднения накопитель гистограммы обнулялся и после
  **последнего** блока, поэтому `energy_occupancy` и итоговое MI считались по
  пустой гистограмме → `occupied_deciles=0`. Это ложно забраковало бы **все**
  прогоны A.5 по критерию годности «занято <4 децилей». Исправлено: обнуление
  пропускается для последнего блока. Обнаружено на калибровочном тайминг-прогоне.
- **Многопоточный BLAS × multiprocessing = oversubscription.** Воркеры без
  ограничения потоков BLAS душили друг друга (7 воркеров → 230% CPU вместо
  ~700%). Решение — однопоточный BLAS + пул по числу физических ядер. Плюс
  осиротевшие spawn-воркеры от убитых прогонов не матчились по `pkill -f
  "experiments.py"` и крали CPU — теперь чищу по `multiprocessing.spawn`.
- **`a1_samplesize` считал MI на N<1000 через `mi_from_hist`**, у которой
  порог `n>=1000` (вернула None для всех N=100..800). Написал локальную
  оценку с той же поправкой на смещение без порога — весь смысл пункта как раз
  в малых N.
- **`c1_pilot` падал дважды:** (1) `nonlocal peak` в функции на уровне модуля
  (нет объемлющей функции) → перешёл на список-контейнер; (2) формат `None`
  для `lr_mean` на вымершем прогоне → добавил защиту. Исправлено, перезапущено.
- **C.3 верхняя граница не откалибрована:** эталонная политика
  `mark_when_hungry_follow_own` не самоподдерживающаяся, наделала 357 меток
  (< порога 1000 в `mi_from_hist`), поэтому MI(состояние; содержание) не
  посчиталась. Механизм самонаправленного чтения при этом подтверждён
  (свежесть 1.0). Нижняя граница (`mark_always` → MI=0) и нулевой уровень
  (`ignore_marks`) откалиброваны. Осталось: либо считать sign-MI малой-N
  оценкой, либо дать политике кормиться (не успел — вынес в следующие шаги).
- **Ночью машина уходила в сон** (ноутбук): блок B шёл ~7 ч настенного
  времени вместо ~1 ч счётного. На числа не влияет (счёт детерминирован),
  но сорвало исходный тайминг; поэтому часть блока B и весь C доделаны утром.

---

## 5. Границы применимости

- Все выводы — на одном мире, одной топологии, одном размере мозга (как и
  заявлено в предрегистрации §9). Обобщение за их пределы не заявляется.
- **Мощность снижена железом.** A.5 — 12 seed × 120k вместо 20 × 200k
  (2-ядерная машина). Заявленная чувствительность пересчитана: 2σ=0.050 бит
  внутриагентно. Эффект `self`−`shuffled` (0.023) в этот порог не проходит,
  но при большем числе seed'ов и/или большей длине картина может измениться —
  это ограничение мощности, а не доказательство отсутствия эффекта.
- **Метрика не стационарна (A.4).** Даже блочные средние по 15k не выходят на
  устойчивое плато к 400k (1 из 3 seed'ов растёт, 1 падает). Любое одиночное
  измерение MI на этом мире несёт компоненту фазы бум-крах-цикла. Наши выводы
  устойчивы только в среднем по блокам и seed'ам, но не по отдельному прогону.
- **Прибор слеп вне занятого диапазона энергий.** При концентрации популяции
  (напр. threshold@20–30) занято <4 децилей и метрика физически не видит
  переключения. В A.5 все годные прогоны занимали 8 децилей — здесь ок.
- **Внутриагентная метрика при малых N шумна.** Поправка на случайность для
  таблицы 10×8 при N=200 составляет ~0.23 бит — больше сигнала эволюционной
  популяции (~0.05). Среднее по многим агентам несмещённо, но per-agent оценки
  при `min_samples`≈200 ненадёжны; для per-agent выводов нужно N≥400–800.
- **Социальный слой энергетически несбалансирован.** `give` создаёт +4 чистой
  энергии на событие, поэтому кооперация связана с ростом популяции и валидн.
  прогона. Любые выводы о кооперации на текущем движке несут этот артефакт;
  тест Requejo–Camacho на нём некорректен без нейтрализации give.
- **Контроль `off` (зануление) хуже `shuffled` (подмена)** — подтверждено
  измерением (`off` даёт даже более высокую MI). Основной контроль — только
  `shuffled`.

## 6. Следующие шаги (развилки, не «сделать лучше»)

1. **Развязать кооперацию и энергетику (блок B).** Сделать `give`
   энергетически нейтральным (give_gain = give_cost) или ввести отдельный
   «социальный» ресурс, не влияющий на метаболизм. Только тогда свип
   regrowth станет честной проверкой Requejo–Camacho. Развилка: если и после
   этого перехода нет — движок не воспроизводит их результат по существу,
   а не из-за артефакта; если появляется — артефакт был единственной причиной.
2. **Стационарность vs мощность (A.4/A.5).** Либо (а) увеличить число seed'ов
   до ~40–60, приняв нестационарность как шум и давя её усреднением, либо
   (б) искать параметры мира с меньшими бум-крах-колебаниями (выше
   `crowd_cost`, мягче сезоны) — но это уже другой мир и новая предрегистрация.
   Не «просто больше тиков»: A.4 показал, что длина не помогает.
3. **`off` > `shuffled` (неожиданное).** Проверить гипотезу «подменённый
   сигнал — активный шум, размывающий зависимость»: прогнать условие с
   входом 8–9 = константа (не ноль, не подмена) и посмотреть, где оно ляжет.
4. **Мощность A.5.** Догнать до преригестрированных 20 seed × 200k на машине
   с ≥8 физическими ядрами и проверить, не пройдёт ли `self` vs `shuffled`
   порог 2σ при полной мощности. Сейчас парный сигнал есть (p=0.0005), не
   хватает именно числа seed'ов против межсидового шума.
